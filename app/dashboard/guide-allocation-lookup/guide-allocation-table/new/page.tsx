"use client";

import { useParams } from "next/navigation";
import Main from "@/app/components/guide-allocation-lookup/guide-allocation-table/main";

import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
} from "@/components/ui/card";

import {
  Breadcrumb,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbList,
  BreadcrumbPage,
  BreadcrumbSeparator,
} from "@/components/ui/breadcrumb";

import { Separator } from "@/components/ui/separator";
import { ModuleDetailsString } from "@/app/(DashboardLayout)/types/module-details/ModuleDetails";


  const BCrumb = [
    { href: "/", title: "Guide Allocation Table" },
    { title: "Add New Guide Allocation Table" },
  ];

const Page = () => {
  const params = useParams();
  const id = params?.id;
  const recordid = params?.recordid;

  const objModule: ModuleDetailsString = {
    moduleId: 1,
    keyWord: "guide_allocation_table",
    subkeyWord: "guide_allocation_lookup",
   serviceId: String(id ?? recordid ?? ""),
    endPoint: "/api/guide-allocation-lookup/guide-allocation-table",
    subDirectoryEndPoint: "/api/guide-allocation-lookup/guide-allocation-table",
    tableEndPoint: "/api/guide-allocation-lookup/guide-allocation-table",
    isNew: true,
    isListView: false,
  };

  return (
    <div className="p-6 space-y-6">


 
      <Card>
        <CardHeader className="text-center">
          <CardTitle className="text-2xl font-bold">Add New Guide Allocation </CardTitle>
           
        </CardHeader>

        <CardContent>
          <Main module={objModule} />
        </CardContent>
      </Card>

    </div>
  );
};

export default Page;
