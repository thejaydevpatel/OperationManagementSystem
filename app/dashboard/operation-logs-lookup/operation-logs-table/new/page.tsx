"use client";

import { useParams } from "next/navigation";
import Main from "@/app/components/operation-logs-lookup/operation-logs-table/main";

import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
} from "@/components/ui/card";

import {
  Breadcrumb,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbList,
  BreadcrumbPage,
  BreadcrumbSeparator,
} from "@/components/ui/breadcrumb";

import { Separator } from "@/components/ui/separator";
import { ModuleDetailsString } from "@/app/(DashboardLayout)/types/module-details/ModuleDetails";


  const BCrumb = [
    { href: "/", title: "Operation Logs Table" },
    { title: "Add New Operation Logs Table" },
  ];

const Page = () => {
  const params = useParams();
  const id = params?.id;
  const recordid = params?.recordid;

  const objModule: ModuleDetailsString = {
    moduleId: 1,
    keyWord: "operation_logs_table",
    subkeyWord: "operation_logs_lookup",
   serviceId: String(id ?? recordid ?? ""),
    endPoint: "/api/operation-logs-lookup/operation-logs-table",
    subDirectoryEndPoint: "/api/operation-logs-lookup/operation-logs-table",
    tableEndPoint: "/api/operation-logs-lookup/operation-logs-table",
    isNew: true,
    isListView: false,
  };

  return (
    <div className="p-6 space-y-6">


 
      <Card>
        <CardHeader className="text-center">
          <CardTitle className="text-2xl font-bold">
             Add New Operation Log
          </CardTitle>
        </CardHeader>

        <CardContent>
          <Main module={objModule} />
        </CardContent>
      </Card>

    </div>
  );
};

export default Page;
